import 'package:flutter/material.dart';

class NoteItemTile extends StatelessWidget {
  const NoteItemTile({super.key});

  @override
  Widget build(BuildContext context) {
    return const Text("Note item");
  }
}
